/**
 * Avatar colours.
 *
 * The avatar is the one thing on the page that does not follow the palette by
 * default: agents render white, and only warm to the active signal on hover. That
 * keeps the board quiet until you look at a card, and makes the hover mean
 * something. `USER_AVATAR_HUE` is reserved for personal avatars later.
 */
export const AGENT_AVATAR_COLOR = "#ffffff";
export const USER_AVATAR_HUE = 152;
